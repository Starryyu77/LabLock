# Codex

Install LabLock for Codex with:

```bash
./setup --host=codex
```

This symlinks the repository into `~/.agents/skills/lablock` and enables the Codex-oriented hook template for target projects.
