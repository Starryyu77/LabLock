# Handoff: {{title}}

Generated by LabLock for external AI implementation.

## Project background

{{project_summary}}

Domain: {{domain}}
Current formalism version: {{formalism_version}}

## Coding task

{{coding_task}}

## Research objective to preserve

Experiment: {{exp_id}}
Hypothesis: {{hypothesis}}
Parent: {{parent}}

Primary intervention:
{{primary_intervention}}

Success criteria:
{{success_criteria}}

## Source of truth

Read these before editing:

- `experiments/{{exp_slug}}/hypothesis.md`
- `experiments/{{exp_slug}}/config.yaml`
- `.lablock/locks/{{exp_id}}.scope.lock`
{{additional_sources}}

## Allowed changes

- Experiment-local scripts, configs, notes, and outputs under `experiments/{{exp_slug}}/`.
- Shared code only when it is required to test the hypothesis.
- Tests or probes that directly verify the research effect.

## Avoid

- Do not add broad defensive gates, validators, retry systems, abstractions, or policy checks unless they are required by the experiment.
- Do not reframe the research goal.
- Do not move run outputs outside the experiment folder.

## Expected output

- List the code changes made.
- List the verification command(s) run.
- Write logs/results back under `experiments/{{exp_slug}}/`.
- If the implementation changes the experiment meaning, say so and recommend `/lab-guard`.

## Prompt for the coding agent

Please implement the coding task above while preserving the stated research objective. Optimize for reaching the experiment's intended effect, not for adding extra protective gates. Keep changes small, explain any shared-code edit in terms of the hypothesis, and report verification plus result paths.
