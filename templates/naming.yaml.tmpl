version: 1
profile: {{naming_profile}}
canonical_variable_style: snake_case
experiment_shortname_pattern: "{{experiment_shortname_pattern}}"
matrix_slug_pattern: "{{matrix_slug_pattern}}"
require_variable_registry: {{require_variable_registry}}
require_matrix_registry: {{require_matrix_registry}}
paper_label_required: {{paper_label_required}}
reserved_suffixes:
  - baseline
  - ablation
  - sweep
  - seed
  - debug
  - smoke

